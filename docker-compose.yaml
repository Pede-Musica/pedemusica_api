version: '3.9'

services:
  backend:
    build: ./
    container_name: pedemusica_api
    ports:
      - '3050:3050'
    networks:
      - nestjs_network

networks:
  nestjs_network:
    driver: bridge